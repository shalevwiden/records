services:
  db:
    image: postgres:16
    environment:
      POSTGRES_DB: records
      POSTGRES_USER: records
      POSTGRES_PASSWORD: records
    ports:
      - "5432:5432"
    volumes:
      - records_db_data:/var/lib/postgresql/data

volumes:
  records_db_data:

